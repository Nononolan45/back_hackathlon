version: '3.6'
services:
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
  api: 
    container_name: api
    build: ./back
    ports: 
      - "3000:3000"
    volumes:
      - ./back:/app
    depends_on: 
      - mongo

